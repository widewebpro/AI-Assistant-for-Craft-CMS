{% extends "_layouts/cp" %}
{% set title = "AI Agent Dashboard" %}
{% set selectedSubnavItem = "dashboard" %}

{% block content %}
<div class="ai-agent-dashboard">
    <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 24px;">
        <div class="pane">
            <h3 class="light" style="margin-bottom: 4px;">Total Conversations</h3>
            <p style="font-size: 2em; font-weight: 600; margin: 0;">{{ totalConversations }}</p>
        </div>
        <div class="pane">
            <h3 class="light" style="margin-bottom: 4px;">Active</h3>
            <p style="font-size: 2em; font-weight: 600; margin: 0; color: #16a34a;">{{ activeConversations }}</p>
        </div>
        <div class="pane">
            <h3 class="light" style="margin-bottom: 4px;">Escalated</h3>
            <p style="font-size: 2em; font-weight: 600; margin: 0; color: #dc2626;">{{ escalatedConversations }}</p>
        </div>
        <div class="pane">
            <h3 class="light" style="margin-bottom: 4px;">Total Messages</h3>
            <p style="font-size: 2em; font-weight: 600; margin: 0;">{{ totalMessages }}</p>
        </div>
    </div>

    {% if totalChunks %}
    {% set vectorsMissing = totalChunks - vectorCount %}
    <div class="pane" style="margin-bottom: 24px;{{ vectorsMissing > 0 ? ' border-left: 3px solid #dc2626;' : '' }}">
        <h3 class="light" style="margin-bottom: 4px;">Knowledge Base Vectors</h3>
        <p style="font-size: 2em; font-weight: 600; margin: 0;{{ vectorsMissing > 0 ? ' color: #dc2626;' : '' }}">{{ vectorCount }} <span class="light" style="font-size: 0.5em; font-weight: 400;">of {{ totalChunks }} chunks</span></p>
        <p class="light" style="margin: 4px 0 0; font-size: 12px;">
            for <code>{{ embeddingModel }}</code>.
            {%- if vectorsMissing > 0 %} {{ vectorsMissing }} chunk(s) have no vector for this model — reprocess those files in <a href="{{ cpUrl('ai-agent/settings/knowledge-base') }}">Knowledge Base</a>, or semantic search silently falls back to keyword matching for them.{% endif %}
        </p>
    </div>
    {% endif %}

    <div class="pane">
        <h2>Recent Conversations</h2>
        {% if recentConversations | length %}
        <table class="data fullwidth">
            <thead>
                <tr>
                    <th>Session ID</th>
                    <th>Page</th>
                    <th>Status</th>
                    <th>Date</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for conversation in recentConversations %}
                <tr>
                    <td><code>{{ conversation.sessionId[:8] }}...</code></td>
                    <td>{{ conversation.pageUrl ?? '—' }}</td>
                    <td>
                        <span class="status {{ conversation.status == 'active' ? 'live' : (conversation.status == 'escalated' ? 'expired' : '') }}"></span>
                        {{ conversation.status | capitalize }}
                    </td>
                    <td>{{ conversation.dateCreated }}</td>
                    <td><a href="{{ cpUrl('ai-agent/conversations/' ~ conversation.id) }}">View</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% else %}
        <p class="light">No conversations yet. Enable the widget in <a href="{{ cpUrl('ai-agent/settings') }}">Settings</a> to get started.</p>
        {% endif %}
    </div>
</div>
{% endblock %}
